<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.taphan.core1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/chatsibot_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".login.ResetPasswordActivity"/>
        <activity android:name=".chat.ChatActivity"
            android:label="Csdfsdfsdkfj"/>

        <activity android:name=".login.SignupActivity"/>
        <activity android:name=".course.AddCourseActivity"/>
        <activity android:name=".course.AppInfoActivity"/>
        <activity android:name=".ProfActivity"/>
        <activity android:name=".course.InfoActivity"/>
    </application>


</manifest>